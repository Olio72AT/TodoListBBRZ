@model IEnumerable<TODOListAP03.ViewModels.TodosDetailsViewModel>

@{
    ViewBag.Title = "ShowViewModel";
}

<h2>ShowViewModel</h2>

<!-- Comment out Razor ... Create new, because its only to display -->
@*
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
*@

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nummer)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Kurzbeschreibung)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DetailsId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Beschreibung)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Done)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.Nummer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Kurzbeschreibung)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DetailsId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Beschreibung)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Done)
            </td>
            <td>
                <!-- Comment out Razor ... Create new, because its only to display -->
                @*
        @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
        @Html.ActionLink("Details", "Details", new { id = item.Id }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                *@
            </td>

        </tr>
    }

</table>
<p> 
    Beispiel einer 1:1 Beziehung in Darstellung eines ViewModels. <br /> 
    
</p>
